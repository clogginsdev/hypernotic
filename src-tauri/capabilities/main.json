{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability2",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": ["dialog:default", "dialog:allow-open"]
}
